---
phase: 01-live-foundation-design-system-hero
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - public/fonts/ClashDisplay-Semibold.woff2
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/providers.tsx
autonomous: false
requirements: [DSYS-01, DSYS-02, DSYS-04]
user_setup:
  - service: fontshare
    why: "Clash Display is owner-self-hosted under the ITF Free License; the .woff2 cannot be fabricated by the agent"
    dashboard_config:
      - task: "Download Clash Display Semibold (weight 600) .woff2 and place at public/fonts/ClashDisplay-Semibold.woff2"
        location: "fontshare.com/fonts/clash-display (ITF Free Font License)"

must_haves:
  truths:
    - "All design colors, gradient stops, and radius values resolve from the single @theme inline block in globals.css (no hardcoded hex elsewhere)"
    - "Clash Display, Inter, and JetBrains Mono are self-hosted via next/font and applied through CSS variables on <html> (no third-party font request)"
    - "Every Motion animation auto-disables under prefers-reduced-motion via the global MotionConfig"
  artifacts:
    - path: "src/app/globals.css"
      provides: "@theme inline token block: --font-*, --color-* (bg/surface/border/text/text-muted/violet/blue/cyan), --radius-* + reduced-motion CSS guard"
      contains: "@theme inline"
    - path: "src/app/layout.tsx"
      provides: "next/font wiring (localFont Clash Display 600 + Inter + JetBrains Mono), variable classNames on <html>, metadata"
      contains: "localFont"
    - path: "src/components/providers.tsx"
      provides: "client MotionConfig reducedMotion=user wrapper"
      contains: "MotionConfig"
  key_links:
    - from: "src/app/layout.tsx"
      to: "<html> className"
      via: "clashDisplay.variable inter.variable mono.variable applied"
      pattern: "\\.variable"
    - from: "src/app/globals.css"
      to: "next/font CSS variables"
      via: "@theme inline resolves --font-display/--font-sans/--font-mono"
      pattern: "@theme inline"
    - from: "src/components/providers.tsx"
      to: "children"
      via: "MotionConfig reducedMotion=user wraps app"
      pattern: "reducedMotion"
---

## Phase Goal

**As a** recruiter or hiring manager, **I want to** land on a fast, on-brand hero that names Jacky, his identity, and his builder arc with clear next-step CTAs, **so that** within seconds I can tell he is a serious builder worth interviewing.

> **This plan:** lock the design-system foundation (DSYS-01/02/04). Tokens and fonts must exist before any primitive styles against them (Walking-Skeleton sequencing step 2).

<objective>
Establish the single design-token source (`@theme inline` in `globals.css`), self-host all three fonts through `next/font` with zero layout shift, and install the global reduced-motion gate. After this plan the live placeholder renders in the brand fonts and dark palette.

Purpose: DSYS-01 (one token source), DSYS-02 (self-hosted fonts, no FOUT/FOIT/CLS), and the DSYS-04 reduced-motion half are structural foundations that every primitive and the hero depend on. The `inline` keyword and the `withBasePath`-free `next/font` paths prevent two of the five known pitfalls (unresolved font vars, FOUT).
Output: Resolvable design tokens, three self-hosted fonts wired via CSS variables, and a `<MotionConfig reducedMotion="user">` wrapper in the root layout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-live-foundation-design-system-hero/01-UI-SPEC.md
@.planning/phases/01-live-foundation-design-system-hero/01-RESEARCH.md
@.planning/phases/01-live-foundation-design-system-hero/01-PATTERNS.md

<interfaces>
<!-- Copy-from snippets exist in the source artifacts — use verbatim, do not re-derive. -->
- `src/app/layout.tsx` fonts — RESEARCH §"Pattern 2: Three fonts → one @theme inline block" + PATTERNS §`layout.tsx`. `localFont({ src:[{ path:"../../public/fonts/ClashDisplay-Semibold.woff2", weight:"600", style:"normal" }], variable:"--font-display", display:"swap" })`; `Inter({ subsets:["latin"], variable:"--font-sans", display:"swap" })`; `JetBrains_Mono({ subsets:["latin"], variable:"--font-mono", display:"swap" })`. Apply `${clashDisplay.variable} ${inter.variable} ${mono.variable}` on `<html lang="en">`. Load weight **600 only** (Bold 700 reserved — UI-SPEC Typography).
- `src/app/globals.css` tokens — RESEARCH §"Pattern 2" + PATTERNS §`globals.css` (literal block) + UI-SPEC Color/Spacing/Radius tables. `@import "tailwindcss";` then `@theme inline { --font-display:var(--font-display); --font-sans:var(--font-sans); --font-mono:var(--font-mono); --color-bg:#0A0A0F; --color-surface:#14141F; --color-border:#262633; --color-text:#EDEDF2; --color-text-muted:#A1A1B5; --color-violet:#7C3AED; --color-blue:#3B82F6; --color-cyan:#22D3EE; --radius-sm:6px; --radius-md:10px; --radius-lg:16px; --radius-pill:9999px; }`. The `inline` keyword is REQUIRED to resolve next/font vars (Pitfall 4). Add `@media (prefers-reduced-motion: reduce){ .ambient-glow{ animation:none } }` (Pattern 5 belt-and-suspenders guard for the CSS-keyframe glow used by the hero in 01-04).
- `src/components/providers.tsx` — RESEARCH §"Pattern 5". `"use client"`; import `{ MotionConfig }` from `motion/react`; export a component wrapping `{children}` in `<MotionConfig reducedMotion="user">`. Imported by the RSC `layout.tsx` around `{children}` (MotionConfig is client-only — keep layout itself a Server Component).
- Spacing: use Tailwind v4 default 4px scale (xs4/sm8/md16/lg24/xl32/2xl48/3xl64 per UI-SPEC) — no extra token declarations needed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The font-wiring task needs the Clash Display Semibold file present; `next/font/local` resolves its path at build and the export build fails if the file is missing.</what-built>
  <how-to-verify>
    1. Download **Clash Display — Semibold (weight 600)** as `.woff2` from `fontshare.com/fonts/clash-display` (ITF Free Font License — free for commercial use; owner action item #2 — PATTERNS Owner Action Items / RESEARCH OQ-2).
    2. Place it at exactly `public/fonts/ClashDisplay-Semibold.woff2`.
    3. Confirm it is a valid woff2 (begins with the `wOF2` signature) and is the 600 weight only (Bold 700 is reserved this phase).
  </how-to-verify>
  <verify>
    <automated>test -f public/fonts/ClashDisplay-Semibold.woff2 && head -c 4 public/fonts/ClashDisplay-Semibold.woff2 | grep -q 'wOF2' && echo "FONT_OK"</automated>
  </verify>
  <resume-signal>Type "approved" once `public/fonts/ClashDisplay-Semibold.woff2` exists and is a valid woff2, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Self-host all three fonts + global reduced-motion provider</name>
  <files>src/app/layout.tsx, src/components/providers.tsx</files>
  <action>
    Create `src/components/providers.tsx` per the `<interfaces>` snippet: a `"use client"` component that wraps `{children}` in `<MotionConfig reducedMotion="user">` (import from `motion/react`) — this is the single global gate that disables every Motion animation under prefers-reduced-motion (DSYS-04); do NOT hand-roll per-component `useReducedMotion`. Rewrite `src/app/layout.tsx` (keep it a Server Component): wire `localFont` for Clash Display (Semibold/600 only), and `Inter` + `JetBrains_Mono` from `next/font/google`, each `display:"swap"` with its `variable` per the snippet; apply all three `.variable` classNames to `<html lang="en">`; set `<body className="bg-bg text-text">` (tokens from globals.css); import and render `<Providers>` around `{children}`. Add baseline `metadata` (title/description) — but NO `next/og` (static export, no server runtime). `next/font` paths are basePath-aware automatically — do NOT route them through `withBasePath()`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && grep -q 'localFont' src/app/layout.tsx && grep -q -- '--font-display' src/app/layout.tsx && grep -Eq '\.variable' src/app/layout.tsx && grep -q 'reducedMotion="user"' src/components/providers.tsx && grep -q '"use client"' src/components/providers.tsx && echo "FONTS_MOTION_OK"</automated>
  </verify>
  <done>Build succeeds with the three fonts self-hosted (woff2 emitted under `_next/static/media`); `<html>` carries the three font `.variable` classes; `<Providers>` wraps children with `MotionConfig reducedMotion="user"`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single design-token source in globals.css (@theme inline)</name>
  <files>src/app/globals.css</files>
  <behavior>
    - Build emits utility classes for token-named colors (e.g. `bg-bg`, `text-text-muted`, `from-violet`/`via-blue`/`to-cyan`) — i.e. the `@theme` tokens are picked up by Tailwind v4.
    - `@theme inline` (not bare `@theme`) is used so the injected `--font-display`/`--font-sans`/`--font-mono` next/font vars resolve.
    - A `@media (prefers-reduced-motion: reduce)` rule sets `.ambient-glow { animation: none }`.
  </behavior>
  <action>
    Replace the scaffold `globals.css` body with `@import "tailwindcss";` then the single `@theme inline { ... }` block from the `<interfaces>` snippet — font vars + the full UI-SPEC color palette (`--color-bg/surface/border/text/text-muted/violet/blue/cyan`, exact hex) + radius scale (`--radius-sm/md/lg/pill`). This is the DSYS-01 single source of truth — no design hex is hardcoded anywhere else; primitives and the hero reference these tokens via Tailwind utilities (`bg-bg`, `text-text`, `border-border`, `from-violet via-blue to-cyan`, `rounded-[var(--radius-md)]`, etc.). The `inline` keyword is non-negotiable (Pitfall 4 — without it fonts silently fall back). Append the `@media (prefers-reduced-motion: reduce){ .ambient-glow{ animation:none } }` guard for the hero's CSS-keyframe ambient glow (built in 01-04). Optionally set the page background/`color-scheme: dark` on `body`/`:root` so the dark surface is the default.
  </action>
  <verify>
    <automated>grep -q '@theme inline' src/app/globals.css && grep -q '#0A0A0F' src/app/globals.css && grep -q '#7C3AED' src/app/globals.css && grep -q -- '--radius-lg' src/app/globals.css && grep -q 'prefers-reduced-motion' src/app/globals.css && grep -q 'ambient-glow' src/app/globals.css && npm run build 2>&1 | tail -8 && ls out/_next/static/css/*.css >/dev/null 2>&1 && echo "TOKENS_OK"</automated>
  </verify>
  <done>`globals.css` has one `@theme inline` block carrying all colors/radii/font vars from the UI-SPEC; the dark palette compiles into the exported CSS; the reduced-motion glow guard is present; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → font source | Where font files are fetched from when the page loads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Info disclosure | Font delivery | mitigate | All three fonts self-hosted via `next/font` (local + google build-time self-host) — zero third-party font request, no visitor IP leaked to a font CDN (RESEARCH Security Domain). Fontshare CDN `@import` is explicitly forbidden. |
| T-01-04 | Tampering | Owner-provided `.woff2` | accept | Owner downloads from Fontshare (ITF Free License); file is a static decorative asset with no executable surface; validated as a real woff2 (signature check) before wiring. |
</threat_model>

<verification>
- Build succeeds with three self-hosted fonts (woff2 under `_next/static/media`), proving DSYS-02 wiring.
- The exported CSS contains the dark palette / token values, proving DSYS-01 single-source resolution.
- `MotionConfig reducedMotion="user"` wraps the app and the CSS reduced-motion glow guard is present (DSYS-04 motion half).
</verification>

<success_criteria>
- A single `@theme inline` block is the only source of design colors, gradient stops, and radii (DSYS-01).
- Clash Display + Inter + JetBrains Mono render self-hosted with `display:swap` and no third-party request (DSYS-02).
- Every Motion animation will auto-disable under `prefers-reduced-motion`, and the CSS glow has a matching guard (DSYS-04).
</success_criteria>

<output>
Create `.planning/phases/01-live-foundation-design-system-hero/01-02-SUMMARY.md` when done.
</output>
