---
phase: 01-live-foundation-design-system-hero
plan: 04
type: execute
wave: 4
depends_on: [01-02, 01-03]
files_modified:
  - public/resume.pdf
  - src/components/Hero.tsx
  - src/components/HeroIntro.tsx
  - src/components/Nav.tsx
  - src/app/page.tsx
  - src/app/not-found.tsx
autonomous: false
requirements: [HERO-01, HERO-02, HERO-03, DSYS-03, DSYS-04, LNCH-05]
user_setup:
  - service: resume-asset
    why: "The real resume PDF is owner content the agent cannot author; the Download Resume CTA must not ship a 404"
    dashboard_config:
      - task: "Place the current resume at public/resume.pdf"
        location: "owner-provided file → public/resume.pdf (D-07)"

must_haves:
  truths:
    - "The hero shows 'Jacky Zhang' (gradient h1), the founder kicker, the builder one-liner, and 3 CTAs on the first screen (min-h-[100svh], centered)"
    - "The gradient h1 is a static Server-Component element with NO Motion wrapper and renders at full opacity immediately (LCP)"
    - "Only the kicker / sub-headline / CTA group animates (fade + translate <=8px, <=400ms), inside the global MotionConfig, fully disabled under prefers-reduced-motion"
    - "Download Resume links to a basePath-aware /resume.pdf that resolves on the live URL; Get in Touch opens mailto:jackyz4@illinois.edu; View Work + Nav anchors target #work/#about/#contact and degrade gracefully (no href='#', no console errors)"
    - "The kicker wraps cleanly at 360px without horizontal scroll"
  artifacts:
    - path: "src/components/Hero.tsx"
      provides: "RSC hero: static gradient h1 (LCP) + ambient glow + composes HeroIntro inside min-h-[100svh] centered section"
      contains: "bg-clip-text"
    - path: "src/components/HeroIntro.tsx"
      provides: "client Motion entrance for kicker + sub-headline + 3 CTAs"
      contains: "use client"
    - path: "src/components/Nav.tsx"
      provides: "sticky bar: brand mark + #work/#about/#contact anchors + Resume CTA, composed from primitives"
      exports: ["Nav"]
    - path: "src/app/page.tsx"
      provides: "composes <Nav/> + <Hero/>"
      contains: "Hero"
    - path: "public/resume.pdf"
      provides: "owner-provided resume (D-07)"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/Hero.tsx + src/components/Nav.tsx"
      via: "import + render"
      pattern: "Hero"
    - from: "src/components/Hero.tsx"
      to: "h1 (no Motion)"
      via: "static gradient element, no motion import in this file"
      pattern: "bg-clip-text text-transparent"
    - from: "src/components/HeroIntro.tsx + src/components/Nav.tsx"
      to: "/resume.pdf"
      via: "withBasePath(\"/resume.pdf\") on Download Resume"
      pattern: "withBasePath"
    - from: "src/components/HeroIntro.tsx"
      to: "mailto:jackyz4@illinois.edu"
      via: "Get in Touch CTA"
      pattern: "mailto:jackyz4@illinois.edu"
---

## Phase Goal

**As a** recruiter or hiring manager, **I want to** land on a fast, on-brand hero that names Jacky, his identity, and his builder arc with clear next-step CTAs, **so that** within seconds I can tell he is a serious builder worth interviewing.

> **This plan:** the payload slice — compose the live, LCP-safe styled hero from the design system, then re-verify on the deployed `/jackyzhang` URL. Implements decisions D-01 through D-09. Walking-Skeleton sequencing step 4 + 5.

<objective>
Compose the typographic hero and the nav from the Phase-1 primitives: a static gradient `<h1>` (the LCP element, never animated), a Motion-animated kicker/sub-headline/CTA group, three CTAs (View Work anchor, basePath-aware Download Resume, `mailto:` Get in Touch), and a nav with future-section anchors. Then re-confirm everything on the live GitHub Pages URL.

Purpose: HERO-01 (name + identity + one-liner first screen), HERO-02 (builder arc + 3 CTAs), HERO-03 (LCP renders immediately, gradient drift reduced-motion safe), DSYS-03 (hero/nav composed FROM primitives), DSYS-04 (focus + reduced motion), LNCH-05 (correct paths on the live URL). Implements D-01..D-09.
Output: A live, on-brand hero at `jackychip.github.io/jackyzhang` with working CTAs and no broken paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-live-foundation-design-system-hero/01-CONTEXT.md
@.planning/phases/01-live-foundation-design-system-hero/01-UI-SPEC.md
@.planning/phases/01-live-foundation-design-system-hero/01-RESEARCH.md
@.planning/phases/01-live-foundation-design-system-hero/01-PATTERNS.md

<interfaces>
<!-- Available from 01-01/01-02/01-03 — use directly. -->
- `Button` — `src/components/ui/Button.tsx`: `variant` primary/secondary/ghost, `size` md/sm, `as`/`asChild` to render an `<a>` (forwards href/download/target/rel), optional lucide icon. The 3 CTAs reuse this.
- `Section` — `src/components/ui/Section.tsx`: max-width container + responsive vertical padding (for Nav/Hero layout rhythm).
- `withBasePath` — `src/lib/base-path.ts`: `withBasePath("/resume.pdf")` for the Download Resume `<a href>` (plain anchors to `public/` are NOT auto-prefixed — Pitfall 1).
- `cn` — `src/lib/cn.ts`.
- Global `<MotionConfig reducedMotion="user">` is already mounted in the root layout (01-02) — HeroIntro just uses `motion/react`; no extra reduced-motion plumbing.
- Hero copy/markup — RESEARCH §"Pattern 3" (gradient clip h1) + §"Pattern 4" (LCP-safe split) + PATTERNS §`Hero.tsx`+`HeroIntro.tsx`. h1 classes: `bg-gradient-to-r from-violet via-blue to-cyan bg-clip-text text-transparent font-display font-semibold tracking-[-0.02em] leading-[1.05] text-[clamp(2.75rem,6vw,4.5rem)]`. Section wrapper: `grid min-h-[100svh] place-items-center`.

<!-- Locked content (CONTEXT decisions — implement exactly) -->
- D-01 headline (h1, gradient LCP): `Jacky Zhang`.
- D-02 kicker (JetBrains Mono 14px/400 uppercase, tracking 0.08em, muted): `Founder & Lead Engineer @ Revly · CompE @ UIUC`. MUST wrap cleanly at 360px — do NOT `whitespace-nowrap` (Pitfall 6 / LNCH-01).
- D-03 sub-headline (Inter 16px **weight 600**, solid `#EDEDF2`/text token — NEVER gradient): `I build and ship real things — from a live automotive marketplace to robotics and games played 40,000+ times.`
- D-04 visual: pure typographic — no portrait/code motif; one ambient radial gradient glow, opacity <=0.18, decorative, class `ambient-glow` (its reduced-motion guard is already in globals.css from 01-02).
- D-05 layout: centered single column (kicker → name → one-liner → CTA row), vertically centered in `min-h-[100svh]`.
- D-06 three CTAs: View Work (primary gradient) · Download Resume (secondary outline) · Get in Touch (tertiary ghost).
- D-07 Download Resume → `withBasePath("/resume.pdf")` + `download`.
- D-08 Get in Touch → `mailto:jackyz4@illinois.edu` (the `.edu`, intentionally — NOT the gmail in the account profile).
- D-09 Nav + View Work → in-page anchors `#work`/`#about`/`#contact`; wire now, degrade gracefully on the hero-only page (smooth-scroll or no-op, no `href="#"`, no console errors).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The Download Resume CTA will link to `public/resume.pdf` via `withBasePath()`. The live-URL verification must not ship a 404, so the real file must exist before the final deploy verification.</what-built>
  <how-to-verify>
    1. Place the current resume at exactly `public/resume.pdf` (owner action item #1 — D-07 / RESEARCH OQ-1). Pre-size sensibly; it is a static asset, no optimization needed.
    2. Confirm it is a valid PDF (begins with `%PDF`).
  </how-to-verify>
  <verify>
    <automated>test -f public/resume.pdf && head -c 4 public/resume.pdf | grep -q '%PDF' && echo "RESUME_OK"</automated>
  </verify>
  <resume-signal>Type "approved" once `public/resume.pdf` exists and is a valid PDF, or describe the issue.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: LCP-safe Hero (static gradient h1) + animated HeroIntro + 3 CTAs</name>
  <files>src/components/Hero.tsx, src/components/HeroIntro.tsx</files>
  <behavior>
    - `Hero.tsx` contains a static `<h1>` with `bg-clip-text text-transparent` gradient classes and the text "Jacky Zhang" — and imports NOTHING from `motion` (the LCP element is never wrapped/animated). HARD RULE (HERO-03 / Pitfall 3): no `<motion.h1>`, no `initial={{opacity:0}}` anywhere near the h1.
    - `HeroIntro.tsx` starts with `"use client"`, imports from `motion/react`, and animates ONLY the kicker + sub-headline + CTA group (fade + translate <=8px, <=400ms, slight stagger).
    - Kicker text = D-02 string; sub-headline = D-03 string at Inter 16px/600 solid (no gradient); CTA row = the 3 Buttons (D-06).
    - Download Resume href = `withBasePath("/resume.pdf")` + `download`; Get in Touch href = `mailto:jackyz4@illinois.edu`; View Work href = `#work`.
  </behavior>
  <action>
    Create `src/components/Hero.tsx` as a Server Component (no `"use client"`, no Motion import): the static gradient h1 per the Pattern 3/4 snippet (D-01 "Jacky Zhang"), the single low-opacity (<=0.18) `ambient-glow` radial element (D-04, decorative — its reduced-motion CSS guard already exists in globals.css), all inside `<section className="grid min-h-[100svh] place-items-center">` with a centered single-column stack (D-05); render `<HeroIntro/>` as the sibling block below the h1. Create `src/components/HeroIntro.tsx` (`"use client"`, `motion/react`): the kicker (D-02) — JetBrains Mono 14/400 uppercase tracking 0.08em, muted token, allowed to wrap to two lines at 360px (NO `whitespace-nowrap`; tighten the `·` separator on mobile if needed — Pitfall 6); the sub-headline (D-03) — `font-sans text-base font-semibold text-text`, NEVER gradient; and the CTA row of 3 `Button`s (D-06) rendered as `<a>` via the Button `as`/`asChild` prop: View Work → `#work` (primary), Download Resume → `withBasePath("/resume.pdf")` + `download` (secondary), Get in Touch → `mailto:jackyz4@illinois.edu` (ghost). Animate ONLY this group: fade + translate <=8px, <=400ms, slight stagger — never gate the h1 behind it. The global `MotionConfig reducedMotion="user"` (01-02) disables it under reduced motion automatically.
  </action>
  <verify>
    <automated>grep -q 'bg-clip-text text-transparent' src/components/Hero.tsx && ! grep -Eq 'motion|framer' src/components/Hero.tsx && echo "H1_STATIC_OK"; head -1 src/components/HeroIntro.tsx | grep -q 'use client' && grep -q 'motion/react' src/components/HeroIntro.tsx && grep -q 'withBasePath' src/components/HeroIntro.tsx && grep -q 'mailto:jackyz4@illinois.edu' src/components/HeroIntro.tsx && grep -q '#work' src/components/HeroIntro.tsx && ! grep -q 'whitespace-nowrap' src/components/HeroIntro.tsx && npx tsc --noEmit 2>&1 | tail -5 && echo "HEROINTRO_OK"</automated>
  </verify>
  <done>Hero h1 is a static gradient element with zero Motion in the file; HeroIntro animates only kicker/sub/CTAs; the 3 CTAs carry the correct anchor/`withBasePath` resume/`mailto:` targets; the kicker is not `whitespace-nowrap`; type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Nav + page composition + not-found, then build</name>
  <files>src/components/Nav.tsx, src/app/page.tsx, src/app/not-found.tsx</files>
  <action>
    Create `src/components/Nav.tsx` (UI-SPEC Nav row + D-09): a sticky/fixed top bar on `bg-surface`/translucent surface composed FROM primitives (brand mark "Jacky Zhang" or initials + anchor links `#work`/`#about`/`#contact` + a Resume `Button` CTA reusing `withBasePath("/resume.pdf")`). Active/hover link uses the accent indicator (single-hue, not gradient). Anchors degrade gracefully on the hero-only page — smooth-scroll or no-op, NO `href="#"` placeholders, no console errors (D-09). For any external link rendered now or later, forward `rel="noopener noreferrer"` with `target="_blank"` (establish the convention — RESEARCH Security Domain / T-01-06). Keyboard focus ring on every link (DSYS-04). Rewrite `src/app/page.tsx` to compose `<Nav/>` + `<Hero/>` (this replaces the 01-01 placeholder). Add `src/app/not-found.tsx` as a minimal on-brand 404 (CONTEXT discretion / a11y nicety) and consider a skip-to-content link. Confirm the static export still builds.
  </action>
  <verify>
    <automated>grep -q 'Hero' src/app/page.tsx && grep -q 'Nav' src/app/page.tsx && grep -Eq '#work|#about|#contact' src/components/Nav.tsx && ! grep -q 'href="#"' src/components/Nav.tsx && grep -q 'from "@/lib/cn"\|Button' src/components/Nav.tsx && test -f src/app/not-found.tsx && npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -8 && test -f out/index.html && grep -q 'Jacky Zhang' out/index.html && echo "COMPOSE_OK"</automated>
  </verify>
  <done>`page.tsx` renders Nav + Hero; Nav uses `#work`/`#about`/`#contact` anchors (no `href="#"`) and reuses Button; a not-found page exists; the exported `out/index.html` contains the prerendered "Jacky Zhang" h1.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The full hero + nav are composed and statically export with the gradient h1 prerendered into `index.html`. Pushing to `main` redeploys via the 01-01 Actions pipeline. LNCH-05 is only satisfied by verifying on the LIVE URL.</what-built>
  <how-to-verify>
    1. Push to `main`; wait for the "Deploy to GitHub Pages" workflow to finish green.
    2. Visit `https://jackychip.github.io/jackyzhang/`: the hero shows the gradient "Jacky Zhang", the founder kicker, the one-liner, and the 3 CTAs on the first screen; the name is visible immediately at full opacity (no fade-in on the LCP element).
    3. Click **Download Resume** → it downloads/opens `/jackyzhang/resume.pdf` (200, not 404). Click **Get in Touch** → opens `mailto:jackyz4@illinois.edu`. Click **View Work** / nav anchors → smooth-scroll or no-op, no console errors, no `href="#"` jump.
    4. Fonts are Clash Display / Inter / JetBrains Mono with no visible swap; no `_next/` or asset 404s in the network tab.
    5. Narrow to 360px → no horizontal scroll; the kicker wraps cleanly. Toggle OS "reduce motion" → entrance + ambient glow stop; the h1 is unaffected.
  </how-to-verify>
  <verify>
    <automated>curl -sf -o /dev/null -w "%{http_code}" https://jackychip.github.io/jackyzhang/ | grep -q 200 && curl -sf -o /dev/null -w "RESUME_%{http_code}\n" https://jackychip.github.io/jackyzhang/resume.pdf | grep -q 200 && curl -s https://jackychip.github.io/jackyzhang/ | grep -q 'Jacky Zhang' && echo "LIVE_HERO_OK"</automated>
    <human-check>Live hero renders correctly: gradient name visible immediately, 3 CTAs work, resume 200, fonts swap-free, 360px clean, reduced-motion disables entrance but not the h1.</human-check>
  </verify>
  <resume-signal>Type "approved" once the live hero verifies (CTAs work, resume resolves 200, no 404s, 360px + reduced-motion behave), or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor → outbound links / `mailto:` | The only interactive surface; external navigation + email client launch |
| GitHub Pages → visitor | Static asset (resume.pdf) served publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | External links with `target="_blank"` (Nav / future CTAs) | mitigate | Add `rel="noopener noreferrer"` to every external link (reverse-tabnabbing) — establish the convention on the Nav/Button link path now (RESEARCH Security Domain). |
| T-01-08 | Info disclosure | `public/resume.pdf` + `mailto:jackyz4@illinois.edu` | accept | Both are intentionally public recruiting contact surfaces; owner controls the PDF contents. No sensitive data beyond what the owner chooses to publish. |
| T-01-09 | Info disclosure | Missing security headers on GitHub Pages | accept | Pages serves HTTPS by default; CSP via `<meta http-equiv>` is the only lever and is deferred to the Phase 4 SEO/launch pass (note, do not block). |
</threat_model>

<verification>
- `out/index.html` contains the prerendered gradient "Jacky Zhang" h1 (static SSG — LCP painted on first byte, HERO-03).
- `Hero.tsx` imports no `motion`; HeroIntro animates only the non-LCP group inside the global MotionConfig (HERO-03 + DSYS-04).
- LIVE: hero + 3 working CTAs on the first screen (HERO-01/02); `/jackyzhang/resume.pdf` returns 200 (LNCH-05 + D-07); `mailto:` is the `.edu` (D-08); anchors degrade gracefully (D-09); fonts swap-free; 360px clean (LNCH-01); reduced motion disables entrance + glow but not the h1.
</verification>

<success_criteria>
- Visiting the live URL shows the name, "CompE @ UIUC" identity (founder-forward kicker), the builder one-liner, and the 3 CTAs on the first screen (HERO-01, HERO-02).
- The LCP h1 renders immediately at full opacity; the gradient is confined to the large display heading; any drift stops under prefers-reduced-motion (HERO-03, DSYS-04).
- Hero + Nav are composed from the Phase-1 primitives and all assets (resume.pdf, fonts, `_next/`) load with correct basePath paths on the live URL (DSYS-03, LNCH-05).
- Decisions D-01 through D-09 are all implemented.
</success_criteria>

<output>
Create `.planning/phases/01-live-foundation-design-system-hero/01-04-SUMMARY.md` when done.
</output>
